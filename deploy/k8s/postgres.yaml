apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: vehicle-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: vehicle_db
            - name: POSTGRES_USER
              value: vehicle_user
            - name: POSTGRES_PASSWORD
              value: vehicle_pass
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: vehicle-system
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432

